server:
  port: 8080

spring:
  application:
    name: excel-preview
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

logging:
  level:
    com.example: DEBUG

excel:
  templates:
    user: user_template.xlsx
    organization: organization_template.xlsx
